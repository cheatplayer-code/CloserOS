# Contracts placeholder

This workspace is reserved for a later task that defines versioned contracts.

ADR-0009 makes Pydantic/OpenAPI the initial contract direction and explicitly
defers TypeScript client generation. CLS-001 therefore adds no schemas,
generated clients, or runtime code here.
