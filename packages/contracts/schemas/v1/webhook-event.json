{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://closeros.ai/schemas/v1/webhook-event.json",
  "title": "WebhookEvent",
  "description": "Inbound webhook processing record. Version 1.0.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "id",
    "tenant_id",
    "channel_connection_id",
    "external_event_id",
    "processing_status",
    "received_at",
    "adapter_metadata"
  ],
  "properties": {
    "schema_version": { "$ref": "#/$defs/schemaVersion" },
    "id": { "$ref": "#/$defs/uuid" },
    "tenant_id": { "$ref": "#/$defs/uuid" },
    "channel_connection_id": { "$ref": "#/$defs/uuid" },
    "external_event_id": { "$ref": "#/$defs/externalId" },
    "processing_status": { "$ref": "#/$defs/webhookProcessingStatus" },
    "received_at": { "$ref": "#/$defs/timestamp" },
    "processed_at": {
      "oneOf": [{ "$ref": "#/$defs/timestamp" }, { "type": "null" }]
    },
    "adapter_metadata": { "$ref": "#/$defs/adapterMetadata" }
  },
  "$defs": {
    "schemaVersion": {
      "type": "string",
      "const": "1.0"
    },
    "uuid": {
      "type": "string",
      "format": "uuid"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "pattern": ".*(Z|[+-]\\d{2}:\\d{2})$"
    },
    "externalId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    },
    "adapterMetadata": {
      "type": "object",
      "maxProperties": 32,
      "additionalProperties": {
        "oneOf": [
          { "type": "string", "minLength": 1, "maxLength": 512 },
          { "type": "integer" },
          { "type": "boolean" },
          { "type": "null" }
        ]
      },
      "propertyNames": {
        "type": "string",
        "minLength": 1,
        "maxLength": 64,
        "not": {
          "pattern": "(?i).*(body|text|message|content|token|secret|password|authorization|cookie|email|phone|payload).*"
        }
      }
    },
    "webhookProcessingStatus": {
      "type": "string",
      "enum": [
        "received",
        "acknowledged",
        "processing",
        "processed",
        "failed",
        "dead_letter"
      ]
    }
  }
}
